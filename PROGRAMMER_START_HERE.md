# Programmer — Start here

1. Read `HANDOFF.md`.
2. Restore the complete historical source with `python tools/restore_source.py`.
3. Use the V3 original-layout / V4 motion direction as the design master.
4. Do active development on the `development` branch.
5. Do not connect live payment or expose wholesale/cost data without approval.

Primary production tasks: real catalog data, stock/variants, product imagery, order persistence, checkout/payment, admin tooling, LINE workflow, SEO/analytics and production validation.
