# Development workflow

- `main` — approved / stable handoff baseline.
- `development` — programmer development branch.

New functionality should be developed on `development` or short-lived feature branches and reviewed before merging to `main`.

Current design baseline: V3 original-layout master + V4 motion layer.
