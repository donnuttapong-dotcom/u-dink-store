# Version History

- `v1-original` — first working storefront prototype.
- `v2-expanded-store` — expanded catalog/filter/cart experiment.
- `v3-original-layout-master` — restored original visual layout; approved master direction.
- `v4-motion-source` — V3 master plus motion and interaction layer.

The complete source archive for all versions is stored under `archive/` and can be restored with `tools/restore_source.py`.
